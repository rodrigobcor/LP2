typedef struct {
	int x, y;
	int r;
} circ;

void print (circ* c) {
	printf (
		"Tamanho do raio do circulo e (%d) com centro em (%d,%d)\n",
		c->r, c->x, c->y
	);
}

void main (void) {
	circ c1 = { 1,1, 10,10 };
	print(&c1);
}
